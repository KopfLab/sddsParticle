% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logic_particle.R
\name{particle_store_token}
\alias{particle_store_token}
\alias{particle_get_device_info}
\alias{particle_request_sdds}
\alias{particle_request_sdds_values}
\alias{particle_get_sdds_system}
\alias{particle_get_sdds_command_log}
\alias{particle_send_sdds_commands}
\alias{particle_get_and_parse_sdds_systems}
\alias{particle_get_and_parse_sdds_command_logs}
\title{Interact with the particle API}
\usage{
particle_store_token()

particle_get_device_info(
  token = keyring::key_get("particle"),
  sdds_only = TRUE
)

particle_request_sdds(coreid, token = keyring::key_get("particle"))

particle_request_sdds_values(coreid, token = keyring::key_get("particle"))

particle_get_sdds_system(coreid, token = keyring::key_get("particle"))

particle_get_sdds_command_log(coreid, token = keyring::key_get("particle"))

particle_send_sdds_commands(
  coreid,
  cmds = character(),
  token = keyring::key_get("particle")
)

particle_get_and_parse_sdds_systems(
  core_ids,
  timezone = Sys.timezone(),
  wide = TRUE,
  token = keyring::key_get("particle")
)

particle_get_and_parse_sdds_command_logs(
  core_ids,
  timezone = Sys.timezone(),
  token = keyring::key_get("particle")
)
}
\arguments{
\item{token}{particle access token (retrieved from keyring by default), save a token in your keyring with particle_store_token()}

\item{sdds_only}{whether to only return SDDS devices (default TRUE)}

\item{coreid}{the ID of the particle device to send requests and commands to}

\item{cmds}{SDDS command to send}
}
\description{
Interact with the particle API
}
\section{Functions}{
\itemize{
\item \code{particle_store_token()}: securely stores the access token using your OS keyring

\item \code{particle_get_device_info()}: retrieve the particle SDDS devices registered to your account

\item \code{particle_request_sdds()}: request the self-describing data structure (sdds) from the device, returns a tibble with \code{id}, \code{name}, \code{connected} and \code{return_value} (0 = success)

\item \code{particle_request_sdds_values()}: request the SDDS tree values from the device, returns a tibble with \code{id}, \code{name}, \code{connected} and \code{return_value} (0 = success)

\item \code{particle_get_sdds_system()}: get the sdds system values

\item \code{particle_get_sdds_command_log()}: get the sdds command log from a device, returns a string that's in JSON format, use \link{sdds_parse_command_log} to process

\item \code{particle_send_sdds_commands()}: sends the \code{cmds} to the particle device, returns a tibble with \code{id}, \code{name}, \code{connected} and \code{return_value} (0 = success, > 0 bit-wise indicator of commands that failed, < 0 the exact error code if it was a single command)

\item \code{particle_get_and_parse_sdds_systems()}: get the sdds system values for multiple cores with \link{particle_get_sdds_system} and parse them right away using \link{sdds_parse_system}

\item \code{particle_get_and_parse_sdds_command_logs()}: get the sdds command logs with \link{particle_get_sdds_command_log} for multiple cores and parse them right away using \link{sdds_parse_command_log}

}}
